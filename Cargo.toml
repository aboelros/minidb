[package]
name = "minidb"
version = "0.1.0"
edition = "2021"
description = "A relational database engine built from scratch"
authors = ["Mahmoud"]

[dependencies]
clap = { version = "4.4", default-features = false, features = ["derive", "std", "help", "usage", "error-context"] }
thiserror = "1.0"

[dev-dependencies]
criterion = "0.5"
